## 2024-05-23 - Dialog Interaction Patterns
**Learning:** Input dialogs often miss `autofocus` (requiring an extra tap) and `textCapitalization` (tedious entry).
**Action:** Always add `autofocus: true` to primary dialog inputs and appropriate `textCapitalization` to all text fields.
