## 2024-05-23 - [IconButtons and Accessibility]
**Learning:** `IconButton` widgets in the AppBar frequently miss `tooltip` properties, which is a common accessibility oversight. This significantly impacts screen reader users who rely on these labels to understand the button's function.
**Action:** When adding or reviewing `IconButton`s, especially in AppBars, always verify that a `tooltip` property is present and descriptive.
