## 2024-05-22 - [Optimization] Frontend Code Splitting
**Learning:** Route-based code splitting using `React.lazy` and `Suspense` reduces initial bundle size by loading components only when needed.
**Action:** Use `React.lazy` for routes that are not critical for the initial render, especially those with heavy dependencies like `motion`.
