# Environment Variables for Atomic Habits App
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ========================================
# OPTIONAL: Remote LLM Integration
# ========================================
#
# The app works perfectly with local AI suggestions (no setup needed).
# Configure these ONLY if you want to use a remote LLM endpoint.
#
# The app will automatically fall back to local suggestions if:
# - These variables are not set
# - The remote endpoint is unreachable
# - The request times out (5 seconds)

# Your LLM API endpoint (e.g., OpenAI, Anthropic, or custom proxy)
# LLM_ENDPOINT=https://api.example.com/v1/suggestions

# API key for authentication (if required by your endpoint)
# LLM_API_KEY=your-secret-key-here

# ========================================
# SECURITY WARNING
# ========================================
#
# ⚠️ NEVER commit API keys to version control!
# ⚠️ The .env file is already in .gitignore
# ⚠️ Rotate keys immediately if accidentally exposed
# ⚠️ Use environment-specific keys (dev/staging/prod)

# ========================================
# SETUP INSTRUCTIONS
# ========================================
#
# 1. Copy this file: cp .env.example .env
# 2. Uncomment the variables you need
# 3. Fill in your actual API endpoint and key
# 4. Add flutter_dotenv to pubspec.yaml:
#    dependencies:
#      flutter_dotenv: ^5.1.0
# 5. Load in main.dart:
#    import 'package:flutter_dotenv/flutter_dotenv.dart';
#    await dotenv.load();
# 6. Update ai_suggestion_service.dart to use:
#    dotenv.env['LLM_ENDPOINT']
#    dotenv.env['LLM_API_KEY']

# ========================================
# TESTING
# ========================================
#
# Test without .env file:
# - App should work normally with local suggestions
# - Check DevTools console for "Using local fallback" messages
#
# Test with .env file:
# - Set valid endpoint/key
# - Check DevTools console for "Using remote LLM suggestions"
# - Verify suggestions are contextual and relevant
